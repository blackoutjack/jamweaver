--- closure-orig/src/com/google/javascript/jscomp/CodeGenerator.java	2014-10-03 11:38:06.618298474 -0500
+++ closure/src/com/google/javascript/jscomp/CodeGenerator.java	2014-12-21 14:58:09.666952312 -0600
@@ -334,6 +334,22 @@
         }
         break;
 
+      // JAM begin
+      case Token.TRANSACTION:
+        add("introspect(");
+        add(first);
+        add(")");
+        if (context == Context.STATEMENT) {
+          addNonEmptyStatement(last, Context.OTHER, false);
+        } else {
+          cc.beginBlock();
+          add(last, Context.STATEMENT);
+          cc.endBlock();
+        }
+        Preconditions.checkState(childCount == 2);
+        break;
+      // JAM end
+
       case Token.FUNCTION:
         if (n.getClass() != Node.class) {
           throw new Error("Unexpected Node subclass.");

